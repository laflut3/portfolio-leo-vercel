"use client";

import SectionSign from "@/components/auth/SectionSign";

const ProfilePage: React.FC = () => {
    return (
        <main className="flex min-h-screen flex-col items-center justify-between">
            <SectionSign/>
        </main>
    );
};

export default ProfilePage;
